import React from "react";

function ScheduleAct(props) {
  return (
    <div>
      <p>{props.start}</p>
      <p>{props.act}</p>
      <p>{props.genre}</p>
      <p>{props.genre}</p>
    </div>
  );
}

export default ScheduleAct;
